# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.env

# Data & Cache
data/
NSE.json
api-scrip-master.csv
google scanner dhan/api-scrip-master.csv
*.csv
*.gz
*.zip
*.json
!blacklist.json
!meta.json
# Note: blacklist.json and meta.json are small metadata, but it's better to exclude all .json and only include them if needed.
# Actually, it's safer to exclude ALL .json since token.json and ltp_cache.json are sensitive.
# Let's just exclude the data directory and the master files.

# Exclude all data files and masters
data/
NSE.json
api-scrip-master.csv
google scanner dhan/api-scrip-master.csv
*.csv
*.gz
*.zip
ltp_cache.json
token.json
blacklist.json

# Streamlit Secrets
.streamlit/secrets.toml

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db
